# Subgraph 子图

> 学习日期: 2026-05-06 | 置信度: ⭐⭐⭐⭐

## 核心概念

Subgraph 是可以独立运行的小图，也可以被主图作为一个节点调用。它适合封装复杂子流程，保持主图简洁。

```text
普通节点：一个函数
Subgraph 节点：一整张小图
```

## 关键要点

1. 子图内部 State 不会自动污染主图 State。
2. 主图只接收显式 return 回来的字段。
3. State 字段名与 node 名不能重复。
4. 同一主图体系下，子图通常共享主图 checkpointer，方便恢复完整执行链。
