# 前端复习进度

**最后更新**：2026-07-24

> 独立于 Java 学习线。方法：费曼学习法（合上资料自己讲/写）。
> 计划文档：`前端复习精深计划.md`
> 讲稿沉淀：`前端复习/`

---

## 当前进度

**JS/TS 基础**：✅ 9/9 全部收官
**Vue3**：✅ 9/9 全部收官 🎉
**CSS**：✅ 10/10 全部收官 🎉（盒模型、居中、Flex、Grid vs Flex、BFC、position、z-index、重排重绘、CSS 单位、1px 边框）
**Node.js**：⏳ 2/？ 进行中（事件循环 ✅、模块化 ✅）
**下一主题**：Stream（流式处理）

**Promise 手写**：理解原理完成，**待默写**（合上讲稿默写一遍，卡住的反馈）

---

## 复习顺序与进度

优先级：JS/TS 基础 → Vue3 → CSS → Node.js → React/Next/AI

| 主题 | 状态 |
|------|------|
| JS/TS 基础 | ✅ **完成（9/9）** |
| Vue3 | ✅ **完成（9/9）** |
| CSS | ✅ **完成（10/10）** |
| Node.js | ⏳ **进行中（事件循环 ✅、模块化 ✅）** |
| React/Next/AI | ⬜ 未开始 |

---

## JS/TS 主题明细

| 题 | 内容 | 状态 |
|----|------|------|
| Q1 | 闭包 | ✅ 过关 |
| Q2 | Vue3 ref 原理（类+get/set）| ✅ 过关 |
| Q3 | React useState 闭包陷阱 | ✅ 过关 |
| Q4 | 原型与原型链 | ✅ 过关 |
| Q5 | new 的 4 步 | ✅ 过关 |
| Q6 | this 指向 + call/apply/bind | ✅ 过关 |
| Q7 | 事件循环（宏任务/微任务）| ✅ 过关 |
| Q8 | Promise 手写 | ✅ 原理过关（待默写）|
| Q9 | TS interface vs type、泛型、unknown/any/never | ✅ 过关 |

---

## 2026-07-15 学习记录

**啃下 JS 最硬的一条链**：闭包 → 原型链 → new → this → call/apply/bind → 事件循环 → Promise 手写（进行中）

**过关题目**（7 题完整 + 1 题进行中）：
1. ✅ Q1 闭包：定义、Vue3 ref、React useState 陷阱、防抖节流、setTimeout/事件监听
2. ✅ Q2 原型链：prototype vs `__proto__`、原型链查找、toString 来源
3. ✅ Q3 new 的 4 步：造对象、接原型链、call 绑 this、返回对象
4. ✅ Q4 this 指向：4 种绑定优先级（new > 显式 > 隐式 > 默认）、箭头函数
5. ✅ Q5 call/apply/bind：传参方式、执行时机、返回值
6. ✅ Q6 事件循环：宏任务/微任务队列、执行顺序、async/await
7. ⏳ Q7 Promise 手写：状态机、异步 resolve、回调数组、微任务包装（70%完成）

**关键收获**：
- 这 7 块是环环相扣的：new 用到原型链和 call，call 属于 this 显式绑定，事件循环解释了 Promise.then 为什么是微任务，Promise 手写用到状态机和事件循环。不是孤立知识点。
- 同一个地基（JS 底层）撑着 Vue 的 ref、React 的 useState、防抖节流。

**关键突破**：
- 从"闭包说反了"到能讲清 6 个场景（setTimeout、事件监听、防抖、ref、useState、箭头函数）
- 纠正了"this 向上找"的错误理解（原型链才向上找，this 看调用方式）
- 事件循环两道题（1432、164235）全对，理解"每个宏任务后清空微任务"

**学习方法验证**：
- "看笔记觉得会 → 合上嘴讲露出缺口"的方法有效
- 两次总结暴露了 new 四步、原型链作用、this 优先级的盲区
- 追问质量高（"call 为什么带参数"、"ref 的 get/set 是 class 自带的吗"）

**过程中的高质量提问**（说明真在动脑）：
- debounce 里 `fn.apply(this, args)` 的 args 是什么
- Vue3 ref 到底长什么样、get/set 是不是 class 自带
- `this.handleClick.bind(this)` 是什么意思
- 为什么 `onFulfilledCallbacks` 要用数组而不是单个变量

**习惯性偏差**（反复出现，需注意）：
- 把"引擎/规则在背后做的事"说成"我/对象主动做"
- 例：说"对象用 `__proto__` 去找"，应该说"JS 引擎顺着 `__proto__` 找"

**自己总结时暴露的缺口（还没完全内化的）**：
- 原型链：第二次总结还漏了"作用是找属性时向上找"
- this：第二次总结还漏了优先级（new > call/bind > 点调用 > 默认）
- new 的四步：第 2、3 步细节模糊（指向 prototype 而非函数本身、call 绑 this）

## 2026-07-21 学习记录（CSS 启动）

**过关题目**（3 题完整）：
1. ✅ Q1 盒模型：`content-box` vs `border-box`，从"完全不知道"到掌握
2. ✅ Q2 居中方案：掌握 5 种方案（Flexbox、Grid、absolute+transform、absolute+margin auto、margin auto）
3. ✅ Q3 Flex 布局：`flex: 1` 三个属性（grow/shrink/basis）+ 计算公式

**关键收获**：
- 理解盒模型：border-box 包含 padding+border，margin 永远不算在 width 里
- 居中方案选择：Flexbox 首选，absolute+transform 适合弹窗
- Flex 计算：`flex: 1` 完全平分，`flex: auto` 基于内容再分配剩余
- 展现批判性思维：发现老师计算错误（300+400+500=1200 超出容器）并主动质疑

**关键突破**：
- 从"不知道"到能独立计算 flex 宽度分配
- 理解 `flex-basis: 0` vs `auto` 的本质区别
- 能选择场景合适的居中方案（不再只知道一两种）

**进度**：CSS 3/10，下次继续 Q4（Grid vs Flex）

**下次计划**：
1. 继续 CSS 剩余 7 题（Grid、BFC、position、层叠、重排重绘、1px、单位）
2. 完成 Promise 手写默写（JS/TS 收尾）

## 2026-07-22 学习记录（CSS 继续：Grid vs Flex + BFC）

**过关题目**（2 题完整）：
4. ✅ Q4 Grid vs Flex：一维 vs 二维、场景选择（5/5 全对）、可以混用
5. ✅ Q5 BFC：独立渲染区域、解决 4 个问题（高度塌陷/margin 重叠/浮动覆盖/穿透）

**关键收获**：
- Grid vs Flex 场景判断：5 个场景全选对（导航栏 Flex、仪表板 Grid、文章 Flex、卡片墙 Grid）
- 理解一维 vs 二维的本质区别
- BFC 能解决 4 类布局问题，记住"给受害者加 BFC"
- 理解 `overflow: hidden` vs `display: flow-root` 的权衡

**关键突破**：
- 从"不知道"到能准确判断 Grid/Flex 使用场景
- 理解 BFC 的"隔离罩"本质，能诊断 4 类问题
- 学会"给谁加 BFC"的判断（给受害者，不是给制造问题的）

**盲区纠正**：
- ⚠️ Q5 场景 3：给侧边栏加 BFC → 应该给主内容区加（被覆盖的是受害者）
- ⚠️ Q5 场景 4：包一层加 BFC → 直接给父元素加就行（多此一举）

**AI 时代视角**：
- 新增 🤖 视角小节：区分"AI 接管的"vs"更值钱的"vs"学习深度调整"
- Grid/Flex：场景判断和调试能力更值钱，语法细节交给 AI
- BFC：问题诊断和方案选择更值钱，规则细节不用死记

**进度**：CSS 5/10（半程），下次继续 Q6（position 定位）

## 2026-07-22 学习记录（CSS 继续：position + z-index）

**课前小测**（6 题，pre-session-review 队列首次建档）：
- ✅ Q3 BFC 浮动覆盖 → **上次盲区已巩固**（给受害者加 overflow:hidden，理由说对）
- ✅ Q4 Promise 手写核心机制 → 待默写盲区过关（"回调存起来等 settled 再执行"）
- ⚠️ Q5 flex:1 → 值记得（1 1 0%），三属性名淡忘（Hard，07-24 复查）
- ✅ Q6 this 优先级 → 全对
- ⏳ Q1/Q2 position → 预测试，答错正常（正是今日新内容起点）

**过关题目**（2 题完整）：
6. ✅ Q6 position 定位：5 取值 + absolute 锚点规则（3 题练透）
7. ✅ Q7 z-index 层叠上下文：拼爹机制 + 隐形触发 + Portal/Teleport 底层原因

**关键收获**：
- position 5 取值各自"相对谁"：static 不定位 / relative 相对自身原位（不脱流）/ absolute 相对最近已定位祖先（脱流）/ fixed 相对视口 / sticky 粘性
- absolute 锚点规则：向上找**第一个 position 非 static** 的祖先（relative/absolute/fixed/sticky 都算锚），找不到才回退视口
- relative 占坑（不脱流）vs absolute 抽坑（脱流）—— 本质区别
- z-index 不是全局比大小，是"先比父上下文（拼爹）"；99999 只在自己家横
- 隐形触发层叠上下文：opacity<1 / transform / filter / fixed / sticky（实际项目 modal 盖不住的根因）
- 串起 Vue Teleport / React Portal 存在的底层原因（躲开被污染的祖先上下文）

**错题本**（迁移失败 / 概念模糊，复习重点）：
- 🔴 **概念模糊**：absolute "相对谁" 答成"相对自己"（那是 relative）→ 已纠正为"最近已定位祖先"
- 🔴 **规则记反**：absolute 锚点说成"找第一个不是 absolute 的" → 正确是"找第一个不是 static 的"（absolute 本身也能当锚）。同一段代码两条规则给不同答案，是最危险的错法
- 🔴 **概念糅合**：relative/absolute 坑位问题答成"占着+被占据"自相矛盾，且误用"浮动"（那是 float）→ 已拆清：relative 占坑不脱流、absolute 抽坑脱流；float ≠ absolute

**进度**：CSS 7/10，下次继续 Q8（重排重绘 reflow/repaint）
**剩余**：重排重绘、1px 边框、CSS 单位（3 题）

## 2026-07-23 学习记录（CSS 收官：重排重绘 + 单位 + 1px 边框）🎉

**课前小测**（6 题，队列盲区复查全清）：
- ✅ Q3 Flex 三属性名 → 上次 Hard 盲区消除（Hard → Good）
- ✅ Q4 position absolute 锚点 → 上次错过盲区消除（"非 static 祖先"准确）
- ✅ Q5 z-index 拼爹、Q6 Vue3 ref/reactive（RefImpl 说到位）→ 状态良好
- ⏳ Q1/Q2 重排重绘预测试 → Q2"宽度更贵因影响范围大"已答对（摸到核心）

**过关题目**（3 题完整，CSS 全部收官）：
8. ✅ Q8 重排重绘：渲染管线 6 步 + reflow/repaint/composite 三档开销 + 布局抖动
9. ✅ Q9 CSS 单位：em 复利累乘 vs rem 稳定 + vw/vh + % 高度陷阱 + vh 移动端
10. ✅ Q10 1px 边框：DPR + CSS像素vs物理像素 + 伪元素 scaleY 压缩方案

**关键收获**：
- 渲染管线：DOM/CSSOM → Render 树 → **Layout(reflow) → Paint(repaint) → Composite**
- 三档性能开销：几何属性(reflow 最贵) > 外观属性(只 repaint) > transform/opacity(只合成，GPU 最便宜)
- transform 便宜的原因：提升为 GPU 图层"挪贴纸"，坑不动+像素不重画，跳过 ④⑤（跳过是因、快是果）
- 布局抖动（强制同步布局）：读几何值(offsetWidth 等)会逼浏览器立即 reflow → **读写分离**修复
- em 相对父级会逐层累乘（复利失控）；rem 相对根 `<html>`（非 body）永远一把尺
- vw 认视口宽 / vh 认视口高；% 高度需父级有确定高度；vh 移动端跟地址栏打架 → dvh
- 1px 问题根源：DPR≥2 时 1 CSS px = 多个物理像素，细线做不出 → 伪元素 + scaleY(1/DPR)

**错题本**（概念模糊 / 因果倒置，复习重点）：
- 🔴 **判断依据错**：说"background-color 不 reflow 是因为 DOM 没改" → 正确依据是"看改的属性动没动布局"（改 width 不动 DOM 也 reflow）
- 🔴 **因果倒置**：说"transform 性能好所以跳过步骤" → 反了，"跳过 layout/paint 是因，性能好是果"
- 🔴 **概念模糊**：rem 说成"相对 body" → 是根元素 `<html>`（移动端适配设 html 字号的原因）
- 🟡 **计算失误**：100vh 在 1200×800 视口算成 600（抄了宽度）→ vh 认高，应 800
- 🟡 **术语不精**：画线的 `::after` 说成"伪类" → 是伪元素（造元素）；伪类是选状态/位置

**里程碑**：🎉 **CSS 10/10 全部收官**，复习线 JS/TS → Vue3 → CSS 三块完成，下一站 Node.js

## 2026-07-24 学习记录（Node.js 启动：事件循环）

**课前小测**（6 题，方向纠偏：原预测试错出成 Maven，用户指出 Java 在另一台设备学，本会话是 Node 线）：
- ✅ Q2 模块化（require vs import：引入/暴露方式不同）→ 方向对
- ✅ Q3 Stream 大文件（createReadStream 分片读，不挤爆内存）
- ✅ Q5 跨线辨析 Java Stream vs Node Stream（名字同、概念异：Java=集合函数式流水线，Node=I/O 数据管道）→ 避开陷阱
- ✅ Q6 1px 边框（DPR + scaleY，还说了 3x 收 1/3）
- ✅ Q4 Flex 三属性（连续两次过关，H→G，S 延至 8 天）
- ⚠️ Q1 Node 事件循环（"多了 I/O、nextTick 不一样"方向对，缺阶段名）→ 正是今日新内容起点

**过关题目**（1 个大 KP，深入）：
- ✅ Node 事件循环：6 阶段（timers/pending/poll/check/close）+ 双微任务队列 + 确定性判断

**关键收获**：
- Node 事件循环分阶段：timers（setTimeout）→ poll（★ I/O 完成，fs.readFile 回调）→ check（setImmediate）
- 双微任务队列：`process.nextTick` 优先级 > `Promise.then`（浏览器无 nextTick，不存在双队列）
- **确定性判断框架**（核心心法）：看"注册位置"——同轮相邻阶段（poll→check）→ 固定；跨轮/顶层 → 不固定
  - 顶层 setTimeout(0) vs setImmediate → **不固定**（看 1ms 到没到期）
  - I/O 回调里 setImmediate **必赢** setTimeout（poll 之后紧接 check，同轮）
- await 的同步性：`await async2()` 先同步执行 async2 函数体，再暂停当前函数；await 后的代码 = `.then`，是微任务
- 综合执行顺序题方法：分三类排队（同步 → 微任务[nextTick 先] → 宏任务），同步按代码书写顺序

**错题本**（概念模糊 / 结论过度推广，复习重点）：
- 🔴 **结论过度推广**：把"I/O 回调里 setImmediate 必赢"推广到"所有场景 setImmediate 都比 setTimeout 快" → 只在 I/O 回调内成立，顶层不固定。判断依据是"是否同轮相邻阶段"
- 🔴 **await 同步性**：综合题把 B 排在 C 前（答 `...A B...`）→ 应 `A C ... B`。await 先同步执行 async2 体（打印 C），再暂停注册 B 为微任务
- 🔴 **nextTick 漏答**：综合题完全漏了 H（process.nextTick）→ nextTick 永不缺席，且优先于所有 Promise.then
- 🟡 **综合时序**：readFile 回调与顶层 setTimeout/setImmediate 的先后判断混（I/O 比定时器慢，顶层通常先跑；且 readFile 回调内注册的 4 是本轮 check、7 是下轮 timers，4 必在 7 前）

**进度**：Node.js 2/?（事件循环 ✅、模块化 ✅），下次继续 Stream（流式处理）
**剩余 Node 主题**：Stream、HTTP、异步错误处理、Express、数据库、认证

## 2026-07-25 学习记录（Node.js 模块化）

**课前小测**（6 题，队列全覆盖 by pre-session-review）：
- ✅ Q1 模块化（require 运行时 vs import 编译时 + tree-shaking）→ 方向对
- ❌ Q2 `module.exports` vs `exports` 陷阱 → 完全不知道（新内容起点）
- ❌ Q3 JSON 导入差异 → 完全不知道（新内容起点）
- ⚠️ Q4 await 同步性 → **三天连续出错**！答"await 体是微任务"，正确是"await 先同步执行函数体"
- ✅ Q5 BFC（margin 重叠 + 高度塌陷）→ 连续 Good，S 延至 8 天
- ✅ Q6 position 锚点（最近非 static 祖先）→ 连续 Good，已内化

**过关题目**（1 个大 KP，深度）：

✅ **Node 模块化**：CJS vs ESM 6 个必知维度

**关键收获**：

1. **`module.exports` vs `exports` 陷阱**（Q2 盲区→已掌握）
   - `exports` 是 `module.exports` 的别名（引用），`exports = { }` 会断引用
   - 改属性用 `exports.xxx`，替换整个对象用 `module.exports`
   - Node 加载时包一层函数：`(function(exports, require, module, __filename, __dirname) { ... })`

2. **JSON 导入差异**（Q3 盲区→已掌握）
   - `require('./data.json')`：零配置，CJS 自动 JSON.parse
   - `import data from './data.json' with { type: 'json' }`：需显式声明
   - 根因：CJS 运行时加载（看到 .json 自动处理）vs ESM 编译时静态分析（需提前知道类型）

3. **循环引用**（核心对比）
   - CJS：返回**半成品快照**（`require` 时 exports 还没赋值的属性 = `undefined`），静默 bug
   - ESM：**直接报错**（`const` 导出的 TDZ 在读未初始化变量时抛 ReferenceError），fail fast
   - ESM 活绑定（Live Binding）：`import` 不拷值，建立指向导出变量的"指针"，随原变量变化
   - ESM 深度优先求值：依赖先于入口执行（b.mjs 先跑，不是 a.mjs）

4. **动态 `import()` vs 静态 `import`**（串起事件循环）
   - 静态 `import { }`：编译时处理，不返回 Promise，不涉及事件循环
   - 动态 `import()`：运行时发起异步加载，返回 Promise，模块求值在 resolve 时同步执行
   - 同步调用返回 Promise ≠ 代码变微任务（**三天串联的根因**："返回 Promise"和"回调变成微任务"是两个阶段）

5. **类型判定规则**（优先级从高到低）
   - `.mjs`（永远 ESM）> `.cjs`（永远 CJS）> package.json `"type"` 字段 > 默认 CJS

6. **ESM 里缺什么**（全局变量消失）
   - `__dirname`/`__filename` → `import.meta.url` + `fileURLToPath`/`dirname`
   - `require` → `createRequire(import.meta.url)` 可调 CJS/JSON，不能调 ESM
   - 同步不能包异步：`require` 是同步的，ESM 加载涉及异步依赖图分析

**串联事件循环**：
- `import()` 调用同步 → 返回 Promise → resolve/模块求值 → `.then()` 回调入微任务队列
- 与 `await async2()` **完全同模型**：函数体同步执行 → await 暂停 → 后半段 = `.then` 微任务

**错题本**（贯穿三天的事件循环盲区）：
- 🔴 **根本混淆（Day 3 仍出现）**："返回 Promise" = "代码变成微任务" → 这是两个阶段：调用同步返回 Promise，`.then()` 回调才是微任务
  - Day 1：`await async2()` 体答成"微任务"（应是同步）
  - Day 2：预测试 Q4 再犯同一错误
  - Day 3：`import('./a.mjs')` 答"start A end"（应是 start end A），后又问"a.mjs 变成微任务了？"

**进度**：Node.js 2/?（事件循环 ✅、模块化 ✅），下次继续 Stream
**剩余 Node 主题**：Stream、HTTP、异步错误处理、Express、数据库、认证
