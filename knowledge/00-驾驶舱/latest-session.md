# 最近一次学习记录

**最后更新**:2026-08-25（Java 线 Day 30 死信队列 + 延迟消息）

## 2026-08-25 学习记录（Java 线）

### 课前小测（7+1 题）：3 过 + 3 A + 1 H

- ⚠️ Q1 宏微任务（🔴 必查）：微任务列表写全（nextTick + then/catch/finally）✅，但 setImmediate 归属没答（宏任务/check 阶段）→ H，08-28 再查
- ❌ **Q2 受控/非受控（逾期 10 天）：说反了**（"组件中就是非受控"）→ A。file 原因半对（浏览器安全限制 value 只读没说出）
- ❌ Q3 JWT 主动失效：答"重启/修改 token" → A。正解：改密钥（全量失效）/ Redis 黑名单（主流）
- ✅ Q4 事务绑连接：稳（单连接贯穿骨架对）
- ❌ Q5 Express async 错误：结论对（接不到），解法答"加 await" ❌ → A。正解 asyncHandler 包装 + next(err)
- ✅ Q6 RTK Immer：过关（"自动展开去修改，本质造新对象"）
- ✅ Q7 消费组（昨天学）：扩容同组 / 风控另起组，固化快
- ⚠️ Q8 死信预测："做好记录"沾边（真正动机=不丢业务事实）

**诊断**：3 个 A 集中在逾期 10+ 天的 Node/React 老 KP（受控非受控/JWT/Express）——和 08-22 遗忘带同模式：**不是没学会，是太久没提取**。已压 08-27 密集复查。

### 学习成果

**死信队列 + 延迟消息（Day 30）**

**1. 死信队列（DLQ）**：
- 动机：消息=业务事实，丢弃=业务永久丢失（钱扣了货不发）
- 机制：重试 16 次（RocketMQ 默认）失败 → 自动转入 `%DLQ%消费组名`（每组一个死信 Topic，失败按组隔离）
- 本质：**带回收功能的垃圾桶，不是焚化炉**
- 处理三方式：①人工（监控告警+修 bug+重投）②程序化兜底（死信消费者存错误表+通知）③定时自动重投（低峰期，适合临时性失败）
- 闭环：Day 28 三防线第③道（ACK）的延伸——ACK 保"没完成不确认"，死信保"确认失败的也有去处"

**2. 延迟消息**：
- 场景：下单 30 分钟不付自动取消
- 对比扫表：DB 零压力（消息存 MQ）+ 时间精确（不用等扫描周期）+ 天然分布式（多实例无竞争）
- 实现：RocketMQ 4.x 固定 18 个延迟级别（1s~2h，`setDelayTimeLevel(16)`=30min）；5.x 支持任意时间戳（面试加分点）
- **模式精髓**：消费者拿到消息不直接执行业务，**先查当前状态再决定**（30 分钟里世界可能变了）
- 哲学串联：和 React stale closure 同构——消息是发出时刻的快照，执行时重看现实

### 收尾验证（3 问）

Q1 死信三处理方式 ✅ / Q2 延迟消息两优势 ✅ / Q3 为何先查再取消 ⚠️（"状态已变"对，"已消费过"偏——重点是快照 vs 现实）

### 今日面试题沉淀（3 道）

1. 消息重试多次失败怎么办？→ 进死信队列（%DLQ%消费组名，每组隔离）。处理：人工修bug重投 / 程序化兜底存错误表+告警 / 低峰定时自动重投。核心：失败消息不是垃圾，是没完成的工作单
2. 30 分钟未支付自动取消怎么实现？→ 延迟消息：下单发延迟30min消息，MQ到点投递，消费者查订单状态再决定（已支付跳过/未支付取消）。对比扫表：DB 零压力+精确+天然分布式
3. RocketMQ 延迟消息的限制？→ 4.x 只支持固定 18 级（实现是每级别一个定时队列到点搬运），5.x 支持任意时间戳

### 遗留问题 / 下次计划

- **08-27 密集复查 4 条 A/H**：受控非受控（说反）/ JWT 主动失效 / Express asyncHandler / 宏微 setImmediate 归属
- Java 线：微服务阶段接近收官 → 下一阶段运维（Linux/Docker）或实战项目（Java 版 RAG）
- 复习线：⑯ 构建工具链（Vite/Webpack）→ ⑰ Next.js

---

## 上次会话（存档）

**2026-08-24（复习线 ⑭性能优化+⑮测试 双站连过；Java 线 Day 29 RocketMQ 消息模型）**
